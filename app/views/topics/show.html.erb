

<%= link_to '返回列表', :back %></br>
<% @topic.categories.each do |x| %>
<span>[<%= x.name %>]</span>	
<% end %>
<h1><%= @topic.title %></h1>
	<p><%= @topic.content %></p>

	<hr>

	


	<% @comments.each do |c|%>

	<p><%= c.content %></p>
	


	<% end %>

	

	<hr>

	<%= form_for @comment ,:url => topic_comments_path(@topic) do |c|%>
	
	<%= c.label :content ,"留言" %></br>
	<%= c.text_area :content %></br>
  <%= c.submit "發表" , :class => "btn btn_success" %>
 <% end %>
